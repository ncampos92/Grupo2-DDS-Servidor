<div class="form">
  <div class="content-box">
    <center>
      <h1>Iniciar sesión</h1>
    </center>
    <%= form_for :session, url: login_path  do |f| %>
    <div class="field">
      <%= f.label :username, :class => 'login-label' %>
      <%= f.text_field :username, :class => 'input-field' %>
    </div>
    <div class="field">
      <%= f.label :password, :class => 'login-label' %>
      <%= f.password_field :password, :class => 'input-field' %>
    </div>
    <div class="actions">
      <%= f.submit "Login", :class => 'login-form-actions btn btn-primary' %>
    </div>
    <% end %>
  </div>
</div>
